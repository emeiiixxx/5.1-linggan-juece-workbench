# 通用任务输出 HTML 模板生成规范

> 交付对象：产品、Agent、前端、数据及测试团队
>
> 适用范围：所有可在线预览、下载或交付客户的任务节点 HTML 产物
>
> 文档状态：设计规范 v1.0
>
> 核心方案：固定 HTML 渲染器 + 动态结构化内容 + 节点级产物快照

## 1. 文档定位

本规范定义一套通用 HTML 输出系统，而不是一份固定章节的报告模板。

系统必须根据当前任务节点、已完成步骤、用户确认内容、真实来源和已生成资产，动态组成与该节点匹配的 HTML。不同节点共用视觉外壳、交互、安全和渲染规则，但正文结构不得写死。

适用任务包括但不限于：

- 新品企划
- 客户提案
- 主题企划
- 服装设计
- 图案设计
- 调研与趋势分析
- 商品结构规划
- AI 改款结果
- 最终企划案
- 后续新增的任务类型

## 2. 核心原则

1. **节点决定内容**：当前节点输出什么产物，HTML 就展示什么内容。
2. **过程数据驱动**：只能使用任务过程中已产生、已确认或明确标记为假设的数据。
3. **固定渲染器负责版式**：Agent 只输出受约束的 JSON，不自由生成 CSS 或拼接任意 HTML。
4. **产物按版本追加**：重新调研、重新规划或重新生成时创建新版本，不覆盖旧版本。
5. **客户交付默认无平台品牌**：不输出平台方名称、Logo、署名或 Powered by 信息。
6. **客户品牌必须明确提供**：只有输入存在 `client_brand` 时才显示客户品牌；为空时隐藏整个品牌位。
7. **事实与推测分离**：事实需要来源，推测需要标记，趋势预测需要可行性说明。
8. **完整保留生成结果**：AI 结果类产物必须保留所有成功生成批次，并标记选中状态与历史状态。
9. **正文服务当前决策**：最终企划正文优先展示已确认内容，未采用或历史内容进入附录。
10. **同一份内容用于预览与下载**：在线预览和下载文件不得出现内容或版本差异。

## 3. 非目标

本规范不负责：

- 在导出 HTML 内继续编辑任务数据。
- 让 Agent 每次重新设计页面视觉。
- 根据业务档案名称、参考品牌或竞品名称猜测客户品牌。
- 将社媒互动量、电商评论量或搜索热度直接解释为销量。
- 将未来节点尚未生成的内容提前填入当前产物。
- 删除或覆盖任务历史版本。

## 4. 推荐架构

```text
任务过程状态
  ↓
节点产物适配器 Node Artifact Adapter
  ↓
通用文档模型 UniversalReportDocument
  ↓
章节解析器 Section Resolver
  ↓
固定 HTML 渲染器 Universal HTML Renderer
  ↓
在线预览 / HTML 下载 / 后续 PDF、PPT 转换
```

### 4.1 职责分工

任务服务：

- 保存任务节点、版本、用户输入、确认结果和生成资产。
- 为每次输出提供稳定的任务快照。

Agent：

- 将任务快照整理为受约束的结构化内容。
- 标记事实、推测、可信度、可行性和数据缺口。
- 不输出自由 HTML、CSS、脚本或 iframe。

前端渲染器：

- 根据 `section.type` 选择固定组件。
- 负责版式、主题、响应式、动画、转义、可访问性和下载。
- 对未知章节类型显示安全降级提示，不执行未知内容。

## 5. 通用文档数据模型

```json
{
  "schema_version": "1.0",
  "document": {
    "artifact_id": "",
    "task_id": "",
    "project_id": "",
    "workflow_type": "new_product | client_proposal | theme_plan | apparel_design | pattern_design | research | custom",
    "node_type": "research | direction | structure_plan | generation_result | final_plan | custom",
    "artifact_type": "research_report | direction_report | structure_plan | ai_result | design_result | final_plan | proposal | custom",
    "version": 1,
    "parent_artifact_id": "",
    "status": "draft | confirmed | superseded | complete | partial | failed",
    "title": "",
    "subtitle": "",
    "language": "zh-CN",
    "client_brand": "",
    "generated_at": "",
    "snapshot_at": ""
  },
  "task_context": {
    "original_request": "",
    "latest_revision_request": "",
    "market": [],
    "season": [],
    "category": [],
    "audience": [],
    "price_range": [],
    "channel": [],
    "constraints": [],
    "exclusions": [],
    "confirmed_direction_ids": []
  },
  "sections": [],
  "assets": [],
  "sources": [],
  "data_gaps": [],
  "history": []
}
```

### 5.1 字段规则

- `artifact_id`：每次导出产物的唯一标识。
- `version`：同一节点每次重新执行后递增。
- `parent_artifact_id`：指向上一版本，形成可追溯链路。
- `status=partial`：任务只完成部分内容时使用，不得伪装为完整报告。
- `status=superseded`：历史版本保留但不再作为当前结果。
- `client_brand`：只接受客户明确提供的自有品牌名称。
- `latest_revision_request`：保存触发本版本的用户调整或补充内容。
- 所有空数组保持为空，不用虚构内容填充。

## 6. 动态章节模型

`sections` 使用受约束的可辨识联合类型。每个章节至少包含：

```json
{
  "id": "section-01",
  "type": "rich_text",
  "title": "",
  "eyebrow": "",
  "description": "",
  "status": "confirmed | suggested | historical | incomplete",
  "visibility": "main | appendix | hidden",
  "data": {}
}
```

允许的基础章节类型：

| `section.type` | 用途 |
|---|---|
| `hero` | 封面、任务摘要、关键元信息 |
| `summary` | 执行摘要或节点结论 |
| `rich_text` | 结构化正文，不接受原始 HTML |
| `metrics` | 可追溯的数字指标 |
| `research_scope` | 市场、平台、人群、关键词和排除项 |
| `evidence_list` | 研究证据、可信度和局限 |
| `direction_cards` | 趋势或视觉方向 |
| `structure_matrix` | 商品层级、角色、波段和渠道结构 |
| `assortment_table` | 品类、款数、价格和 SKU 规划 |
| `image_gallery` | 普通参考图或成果图集 |
| `image_batch_gallery` | 按生成批次展示全部 AI 图片 |
| `comparison` | 版本、方向或方案对比 |
| `moodboard` | 主题情绪板 |
| `color_palette` | 色彩系统 |
| `material_board` | 面料、辅料和细节方向 |
| `timeline` | 调研、开发、上新或交付节奏 |
| `source_list` | 来源与原始链接 |
| `assumptions` | 假设、风险和待验证项 |
| `generation_log` | 生成批次、失败和重试记录 |
| `appendix` | 历史版本、未采用方案和补充材料 |

未知 `section.type` 必须降级为“当前版本暂不支持显示此章节”，不得执行其中内容。

## 7. 节点与内容映射

### 7.1 调研与趋势节点

必须包含：

- `hero`
- `research_scope`
- `evidence_list`
- `summary`
- `source_list`
- `assumptions`

存在视觉方向时可增加：

- `direction_cards`
- `image_gallery`
- `color_palette`

### 7.2 视觉方向确认节点

必须包含：

- 用户已确认方向
- 未确认候选方向
- 每个方向的设计语言、适用品类和证据
- 用户补充内容及本轮变化

被淘汰方向可以进入附录，但不得继续表现为可选状态。

### 7.3 商品结构规划节点

必须包含：

- `summary`
- `structure_matrix`
- `assortment_table`
- 渠道角色
- 价格层级
- 波段安排
- 结构依据
- 风险和待验证项

不得出现与结构规划无关的固定调研章节。

### 7.4 AI 改款或图片生成结果节点

必须包含：

- 生成目标和已确认约束
- `image_batch_gallery`
- 所有成功生成批次及全部图片
- 每张图片的资产编号、批次、生成类型和状态
- 当前选中图片
- 历史版本和未选中图片
- 失败或中断批次的 `generation_log`

该节点不得只导出用户最终选中的少量图片，否则无法复盘生成过程。

### 7.5 服装设计结果节点

根据实际结果动态包含：

- 系列概念
- 款式、版型和廓形说明
- 商品角色或 SKU 信息
- 面料、辅料和工艺方向
- 款式图、模特图、平铺图、细节图或多视角图
- 设计约束和排除条件
- 全部生成批次及选中状态

### 7.6 图案设计结果节点

根据实际结果动态包含：

- 图案主题和设计逻辑
- 花型类型
- 四方连续或指定位置印花规则
- 色彩和套色
- 输出比例、分辨率和延展方式
- 全部图案结果及生成批次
- 适用品类和落位建议

### 7.7 主题企划节点

根据实际结果动态包含：

- 主题叙事
- 情绪板
- 色彩系统
- 关键廓形
- 面料与细节
- 图案与搭配建议
- 系列节奏
- 已生成视觉资产

### 7.8 最终企划案节点

正文必须优先包含：

- 最终确认的任务范围
- 最终确认方向
- 商品结构与价格
- 用户最终选中的生成结果
- 开发建议
- 渠道与上新节奏
- 来源、风险和数据缺口

附录必须保留：

- 未选中但已成功生成的图片
- 历史调研或结构规划版本
- 用户调整记录
- 失败、中断和重试记录

最终企划案不得将未确认内容包装成最终结论。

### 7.9 客户提案节点

客户提案可以组合调研、视觉、结构、商品与交付计划，但必须根据当前已完成内容组装。未完成模块显示“待补充”或直接隐藏，不得用示例数据填满页面。

## 8. AI 生成资产模型

```json
{
  "asset_id": "asset-001",
  "batch_id": "batch-01",
  "version": 1,
  "type": "apparel | pattern | moodboard | product_image | detail | other",
  "role": "candidate | selected | historical | rejected",
  "status": "success | failed | cancelled | interrupted",
  "title": "",
  "description": "",
  "image_url": "",
  "thumbnail_url": "",
  "width": 0,
  "height": 0,
  "mime_type": "image/jpeg",
  "prompt_summary": "",
  "reference_asset_ids": [],
  "direction_ids": [],
  "created_at": ""
}
```

### 8.1 图片展示规则

- 结果 HTML 使用缩略图展示图集，点击后查看高清图。
- 缩略图和高清图使用独立 URL，避免将超大原图缩成几十像素导致摩尔纹和性能浪费。
- 图片比例由资产类型决定，不强行将服装图、图案图和情绪图裁成同一比例。
- 图案结果和生成过程使用一致比例。
- 图集内部图片不加独立边框；如需圆角，由外层容器统一裁切。
- 所有图片必须有描述性 `alt`。
- `failed`、`cancelled` 和 `interrupted` 不渲染破图，改为状态卡。
- 历史批次保留但不可在导出 HTML 内重新选择。

### 8.2 全部图片与正文体量

- `ai_result`、`design_result`：展示所有成功生成图片。
- `final_plan`、`proposal`：正文展示确认图片，其他图片进入附录。
- 图片较多时按批次分组并延迟加载，不删除历史资产。
- HTML 内不得直接嵌入超大 Base64 图片，除非明确要求完全离线交付。

## 9. 版本与历史规则

1. 每次重新调研、重新规划或重新生成都创建新 `artifact_id`。
2. 新版本通过 `parent_artifact_id` 指向上一版本。
3. 上一版本标记为 `superseded`，但继续保留和可查看。
4. 新版本正文只使用本轮有效数据。
5. 旧版本的选择控件在工作台中禁用；导出 HTML 本身始终只读。
6. 用户本轮补充内容必须记录在 `latest_revision_request`。
7. 多次生成图片按 `batch_id` 分组，不混成无法追溯的单一图集。

## 10. 事实、来源与可信度

事实性调研必须包含：

- 来源名称
- 原始链接
- 来源类型
- 地区
- 采集时间
- 支持的结论
- 局限性
- 可信度百分比

趋势预测必须包含：

- 可行性百分比
- 支持信号
- 反向风险
- 需要补充的验证动作

禁止：

- 补造销量、销售额、增长率或市场规模。
- 将不同平台数据合并成虚构综合指标。
- 将社媒互动、电商评论、商品陈列或搜索热度直接等同销量。
- 删除不利于结论的数据缺口。

## 11. White-label 与品牌规则

- 客户可交付 HTML 默认不显示任何平台方名称、Logo、域名、署名或 Powered by 信息。
- 仅当 `document.client_brand` 有明确值时显示客户品牌。
- `client_brand` 为空时不渲染品牌 DOM，不显示占位文字。
- 业务偏好档案名称不是客户品牌。
- 参考品牌、竞品和来源品牌不是客户品牌。
- 客户品牌必须经过 HTML 转义。
- 客户 Logo 只有在输入提供已授权资源及替代文本时才可使用。

## 12. 视觉与版式

通用外壳采用克制、专业、适合时尚行业交付的编辑式长页结构。

### 12.1 固定视觉能力

- 封面 Hero
- 动态目录
- 章节标题和编号
- 图文混排
- 数据卡和结构表
- 图集、情绪板和对比视图
- 来源、风险和附录
- 桌面、平板和手机响应式
- 打印样式

### 12.2 视觉原则

- 保持清晰层级和充足留白。
- 正文字号不得小于 14px。
- 不使用渐变、玻璃拟态、强阴影或高饱和装饰堆叠。
- 不将平台产品界面的品牌色直接当作客户报告品牌色。
- 客户未提供品牌视觉时使用中性主题。
- 客户提供明确、合法的品牌色时，可映射到受约束的报告主题变量。
- 所有颜色需满足正文可读性要求。

## 13. 交互规则

- HTML 为只读产物，不提供在线编辑。
- 目录支持章节锚点定位。
- 图片支持点击放大、关闭、上一张和下一张。
- 图集 hover 只提供克制反馈，不改变布局。
- 外部来源链接使用 `target="_blank"` 和 `rel="noreferrer"`。
- 动画只允许透明度、轻微位移和轻微缩放复位。
- 动画使用 ease-out，不使用弹跳。
- `prefers-reduced-motion` 下关闭非必要动画。
- 动画脚本失败时内容仍然完整可见。

## 14. 空状态、部分完成与失败

空字段不得补造，按语义选择：

- 未提供
- 暂无可验证数据
- 样本不足
- 待人工确认
- 尚未生成
- 本节点不适用

部分完成：

- 文档状态标记为 `partial`。
- 只展示已完成章节。
- 明确列出未完成项目。

失败或中断：

- 保留原始请求、补充内容和已完成结果。
- 显示失败阶段、可理解的原因和下一步建议。
- 支持详情时提供可复制的问题编号。
- 不显示破损图片或伪造成功状态。

## 15. 安全与隐私

- 对所有动态文本执行 HTML 转义。
- URL 必须经过协议和来源校验。
- 不执行输入数据携带的 HTML、JavaScript、SVG 脚本或事件属性。
- 不允许注入外部 iframe。
- 不自动执行来源网页代码。
- 图片必须来自已授权资源。
- 私有素材不得自动转成公开 URL。
- 不在 HTML 中写入内部 API 地址、访问令牌、用户身份信息或调试日志。
- 外部资源必须符合生产环境 CSP。
- 如生产环境不允许 CDN，使用项目内置依赖或纯 CSS。

## 16. 可访问性与国际化

- HTML 必须包含 `lang`、`charset`、`viewport` 和 `color-scheme`。
- 使用语义化 `main`、`section`、`nav`、`figure`、`table` 和标题层级。
- 所有交互可使用键盘操作。
- 焦点样式清晰可见。
- 图片必须提供 `alt`；纯装饰图使用空 `alt`。
- 不仅依赖颜色表达状态。
- 使用用户选定语言生成标题、正文和状态文案。
- 来源名称、日期和地区在翻译后仍需保持可追溯。

## 17. 性能与文件策略

- HTML 正文、CSS 和必要脚本由固定渲染器生成。
- 图片默认使用外部受控 URL，不内嵌 Base64 原图。
- 首屏主图可优先加载，其余图片使用 `loading="lazy"`。
- 图集使用真实缩略图，点击后加载高清图。
- 避免重复引用同一资源。
- 长图集按批次分组，避免一次渲染过多高清图。
- 在线预览与下载必须引用同一 `artifact_id` 和版本快照。
- 下载文件名应包含任务名、产物类型和版本号。

推荐格式：

```text
{task_name}_{artifact_type}_v{version}_{YYYYMMDD}.html
```

## 18. 渲染流程

```text
1. 获取指定任务节点快照
2. 校验 schema_version
3. 校验 artifact_type 与 node_type
4. 过滤无权限或未授权资产
5. 解析 sections 顺序及 visibility
6. 按 section.type 调用固定渲染组件
7. 对全部动态文本和 URL 执行安全处理
8. 生成目录、章节锚点和附录
9. 注入主题变量、响应式及 reduced-motion 规则
10. 生成单一 HTML 字符串
11. 在线预览和下载共同使用该字符串
```

## 19. Agent 输出约束

Agent 系统提示词至少包含：

```text
你是“任务 HTML 结构化内容生成 Agent”。

你的任务是根据指定任务节点的真实过程数据，输出符合 UniversalReportDocument schema 的 JSON。

你不能自由生成 HTML、CSS、JavaScript 或 iframe。
你只能使用输入中明确提供的事实、图片、用户确认和来源。
缺失数据必须保留为空或标记为未提供，不得补造。
当前节点尚未产生的内容不得提前写入。
事实性结论必须绑定 source_id，并提供可信度。
趋势预测必须标记为预测，并提供可行性。
客户交付内容不得出现平台方品牌。
只有输入明确提供 client_brand 时才能输出客户品牌。
重新执行节点时必须生成新版本，不得覆盖旧 artifact。
```

## 20. 验收用例

### 20.1 通用性

- 调研节点不会出现商品结构规划的固定章节。
- 结构规划节点不会出现无关的调研占位章节。
- AI 改款节点展示所有成功生成批次。
- 图案设计节点可以使用 1:1 图集而不受服装图比例影响。
- 最终企划能够组合此前已确认产物。

### 20.2 历史与版本

- 重新调研后旧报告仍可查看。
- 新报告不覆盖旧 `artifact_id`。
- 旧方向在历史版本中保留，但不再可选。
- 用户补充内容可追溯到触发的新版本。

### 20.3 图片

- AI 结果产物包含所有成功图片。
- 最终企划正文只展示已确认图片，其他结果进入附录。
- 点击缩略图可以查看高清图。
- 图片加载失败时显示状态，不出现破图。
- 缩略图与高清图不使用同一超大资源进行反复缩放。

### 20.4 White-label

- 未提供客户品牌时，HTML 中不存在品牌占位元素。
- 未提供客户品牌时，不出现平台方名称或 Logo。
- 提供客户品牌时只显示该客户品牌。
- 参考品牌和竞品不会被误识别为客户品牌。

### 20.5 事实与安全

- 所有事实结论可以追溯到来源。
- 推测和预测有明确标签。
- 用户输入的 HTML 或脚本只作为转义文本显示。
- 下载文件不包含令牌、内部 API 或调试信息。
- reduced-motion、移动端和打印模式均可正常阅读。

## 21. 最终交付原则

> 外壳保持稳定，内容随节点变化；过程完整保留，结论只使用已确认数据；客户交付默认无平台品牌，所有产物可追溯、可复用、可安全下载。
